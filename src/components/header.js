import React from 'react';

function Header() {
  const headerStyle = {
    backgroundColor: 'black',
    padding: '20px',
    width: '100%',
  };

  return (
    <div style={headerStyle}>
    
    </div>
  );
}

export default Header;