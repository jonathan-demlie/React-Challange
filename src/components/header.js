import React from 'react';

function Header() {
  const headerStyle = {
    backgroundColor: 'black',
    padding: '30px',
    width: '100%',
    margin:'o',
  };

  return (
    <div style={headerStyle}>
    
    </div>
  );
}

export default Header;